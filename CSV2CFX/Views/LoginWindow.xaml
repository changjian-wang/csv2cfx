<ui:FluentWindow x:Class="CSV2CFX.Views.LoginWindow"
                 xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                 xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
                 xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml"
                 xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
                 xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
                 mc:Ignorable="d"
                 Title="CSV2CFX - 用户登录" 
                 Height="450" 
                 Width="400"
                 WindowStartupLocation="CenterScreen"
                 ResizeMode="NoResize">

    <ui:FluentWindow.Resources>
        <Style TargetType="TextBlock">
            <Setter Property="Foreground" Value="{DynamicResource TextFillColorPrimaryBrush}"/>
        </Style>
    </ui:FluentWindow.Resources>

    <Grid Margin="40">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Header -->
        <StackPanel Grid.Row="0" HorizontalAlignment="Center" Margin="0,0,0,30">
            <TextBlock Text="CSV2CFX 系统登录"
                       FontSize="24"
                       FontWeight="Bold"
                       HorizontalAlignment="Center"
                       Margin="0,0,0,10"/>
            <TextBlock Text="请输入您的登录信息"
                       FontSize="14"
                       HorizontalAlignment="Center"
                       Foreground="{DynamicResource TextFillColorSecondaryBrush}"/>
        </StackPanel>

        <!-- Login Form -->
        <StackPanel Grid.Row="1" VerticalAlignment="Center">
            <!-- Username -->
            <TextBlock Text="用户名" 
                       Margin="0,0,0,5"
                       FontWeight="Medium"/>
            <ui:TextBox x:Name="UsernameTextBox"
                        PlaceholderText="请输入用户名"
                        Margin="0,0,0,20"
                        Height="40"
                        FontSize="14"/>

            <!-- Password -->
            <TextBlock Text="密码" 
                       Margin="0,0,0,5"
                       FontWeight="Medium"/>
            <ui:PasswordBox x:Name="PasswordBox"
                            PlaceholderText="请输入密码"
                            Margin="0,0,0,20"
                            Height="40"
                            FontSize="14"/>

            <!-- Role Selection -->
            <TextBlock Text="角色" 
                       Margin="0,0,0,5"
                       FontWeight="Medium"/>
            <ComboBox x:Name="RoleComboBox"
                      Height="40"
                      FontSize="14"
                      Margin="0,0,0,20">
                <ComboBoxItem Content="管理员" Tag="Administrator"/>
                <ComboBoxItem Content="普通员工" Tag="Employee"/>
            </ComboBox>

            <!-- Error Message -->
            <TextBlock x:Name="ErrorMessageTextBlock"
                       Text=""
                       Foreground="Red"
                       FontSize="12"
                       Margin="0,0,0,20"
                       TextWrapping="Wrap"
                       HorizontalAlignment="Center"
                       Visibility="Collapsed"/>

            <!-- Login Button -->
            <ui:Button x:Name="LoginButton"
                       Content="登录"
                       Click="LoginButton_Click"
                       Height="40"
                       FontSize="14"
                       FontWeight="Medium"
                       HorizontalAlignment="Stretch"
                       Appearance="Primary"/>
        </StackPanel>

        <!-- Footer -->
        <TextBlock Grid.Row="2" 
                   Text="CSV2CFX File Upload Tool v1.0"
                   FontSize="10"
                   HorizontalAlignment="Center"
                   Foreground="{DynamicResource TextFillColorTertiaryBrush}"/>
    </Grid>
</ui:FluentWindow>